{# TEMPLATE: DEFAULT PAGE #}

{% extends "base.twig" %}

{% block content %}
	<div class="content-wrapper test">


		<article class="post-type-{{post.post_type}}" id="post-{{post.ID}}">

			<div class = "block lg:hidden">
			<ul class="inline group-hover:block bg-white text-black ml-3 min-[321px]:ml-7 p-4 rounded-xl ">
                                                    <li class="menu-item menu-item-type-taxonomy menu-item-object-category menu-item-264 font-semibold inline text-black mx-5 text-sm min-[321px]:text-base">
                                <a target="_self" href="http://canberramodern.local/about/partners/">Partners</a>    
                            </li>

							         <li class="menu-item menu-item-type-taxonomy menu-item-object-category menu-item-264 font-semibold inline text-black mx-5 text-sm min-[321px]:text-base">
                                <a target="_self" href="http://canberramodern.local/about/patrons/">Supporters</a>    
                            </li>
</div>
			<section class="article-content ml-7 mr-7 md:ml-16 pb-4">
				<h1 class="article-h1 font-bold text-4xl">{{post.title}}</h1>
				<div class="article-body md:w-4/5">
					{{post.content}}
				</div>
			</section>
		</article>
	</div>

	<div
		class="debug p-8 bg-debug hidden">
		{# useful for debugging, delete later #}
		<h1>Debug info</h1>

		<p class="pb-4">post name:
			{{post.name}}</p>

		<p>file name: page.twig</p>

		{# Output what is in the post #}
		{# {{dump(post)}} #}
	</div>

	<!-- /content-wrapper -->
{% endblock %}
